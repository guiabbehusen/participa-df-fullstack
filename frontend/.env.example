# Base da API. Em dev, recomendamos manter /api para usar o proxy do Vite.
VITE_API_BASE_URL=/api

# Para evitar loop de reload por Service Worker, PWA no dev fica desligado.
# Ligue apenas se souber o que está fazendo.
VITE_PWA_DEV=false
